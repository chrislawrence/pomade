<div class="container">
  <div class="today_list">
  <ul>
  {{#each reversedContent}}
    <li>
      {{pomodoro_time startTime}}—{{pomodoro_time endTime}} {{#if tag}}<span class="tag">{{tag}}</span>{{/if}}
      <a {{action deletePomodoro id }} class="delete_pomodoro">X</a>
      </li>
  {{/each}}
  </ul>
  </div>
</div>
