require 'spec_helper'

feature "Working" do
  context "User is logged in" do
    scenario "Starting the timer" do
    end

    scenario "Finishing a pomodoro" do
    end

    scenario "Resetting the timer" do
    end
  end

  context "User not logged in" do
    scenario "Starting the timer" do
    end

    scenario "Finishing a pomodoro" do
    end

    scenario "Resetting the timer" do
    end
  end
end
